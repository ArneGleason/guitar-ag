# AGENTS.md

## Project Summary

This project is a physical-model electric guitar virtual instrument.

The goal is to build a VST3 plugin that synthesizes guitar-like sound using physical modeling, with MPE support so individual notes can bend and respond independently.

This is not a sample-based guitar instrument.

## Current Product Goal

Build an MVP that proves the core concept:

- VST3 plugin loads in a DAW.
- Multiple notes can play at once.
- Each note is generated by an independent modeled string voice.
- MPE per-note pitch bend affects only the intended note.
- Per-note pressure and/or CC74 can modulate the intended voice.
- The output is a clean DI-style electric guitar tone suitable for external amp sims.

## Important Terminology

Use **MPE**, not MPC.

MPE means MIDI Polyphonic Expression. It is required for independent per-note pitch bend and expression.

## Studio Pattern

This project uses the Studio Pattern for lead/reviewer handoffs and multi-machine project memory.

- Version/date: 2026-05-09 initial canonical scaffold
- Source repository: `the-studio-pattern`
- Source commit: `5624928215232b7b82702929a7b9f8f11f19fd5d`
- Source URL: `https://github.com/ArneGleason/the-studio-pattern`
- License: CC BY-SA 4.0
- Local adaptation: keep the guitar-specific engineering rules in this file authoritative, and add lightweight `.agent/` memory surfaces for review cycles, handoffs, and cross-machine state.

Studio Pattern memory lives in:

- `LOCAL_DEV_NOTES.md` — local commands, setup, validation, and workflow notes
- `.agent/PROJECT_LOG.md` — durable project events and Studio Pattern adoption notes
- `.agent/REVIEW_QUEUE.md` — known risks and suggested reviewer focus
- `.agent/session.json` — latest machine-readable session state
- `.agent/handoffs/` — human-readable lead/reviewer or machine handoffs
- `.agent/workflows/` — short resume, suspend, and cross-project feedback recipes

## Core Documents

Read these before making major changes:

- `LOCAL_DEV_NOTES.md` — Studio Pattern local workflow memory
- `.agent/PROJECT_LOG.md` — Studio Pattern project log
- `.agent/REVIEW_QUEUE.md` — Studio Pattern reviewer focus queue
- `PROJECT.md` — plain-language project direction
- `docs/requirements.md` — product requirements
- `docs/architecture.md` — preferred architecture and component boundaries
- `docs/mpe-behavior.md` — MPE behavior and voice-routing rules
- `docs/dsp-notes.md` — physical modeling and tone-generation notes
- `docs/test-plan.md` — testing and manual verification notes
- `DECISIONS.md` — accepted project decisions
- `LEARNINGS.md` — accumulated project learnings
- `plans/` — active or completed implementation plans

## Development Approach

Work in small, verifiable steps.

Prefer small milestones over large rewrites. After each milestone, the project should still build and the current behavior should be manually testable.

Do not introduce large architecture changes unless the current task explicitly calls for it.

Do not race ahead into unrelated features.

## Multi-Machine Workflow

This project is actively worked on from more than one machine, especially a Mac and a Windows PC.

Before starting meaningful work, check whether the local branch is up to date with GitHub. If the local repo is behind, pull first. If there are local changes or the user may have unpushed work on another machine, pause and ask whether anything needs to be committed, pushed, or pulled before continuing.

When finishing meaningful work, prefer leaving changes committed and pushed so the other machine can pull a clean state before the next session.

## MVP Implementation Order

1. Create a minimal VST3 plugin shell.
2. Implement one physical string voice.
3. Add polyphonic voice handling.
4. Add normal MIDI note on/off behavior.
5. Add MPE channel-to-voice tracking.
6. Add independent per-note pitch bend.
7. Add pressure and CC74 expression mapping.
8. Add simple pickup/tone shaping.
9. Add basic UI controls.
10. Add preset/state save and recall.

## DSP Direction

Start with a simple Karplus-Strong or digital waveguide-style string model.

The first goal is musical playability and expressive MPE behavior, not perfect scientific guitar realism.

Avoid sample playback.

Avoid building a full amp/cab/effects suite in the MVP.

## MPE Requirements

In MPE mode:

- Each active note should be tied to its own voice.
- Per-note pitch bend must affect only that voice.
- Per-note pressure should affect only that voice.
- Per-note CC74/timbre should affect only that voice.
- Bend range should be configurable.
- Normal MIDI fallback mode should still be playable.

## Code Quality Rules

- Keep audio/DSP code separate from UI code.
- Keep MIDI/MPE parsing separate from the string model.
- Avoid memory allocation on the audio thread.
- Avoid blocking calls on the audio thread.
- Avoid UI operations on the audio thread.
- Smooth parameter changes to avoid clicks.
- Prefer clear, testable code over clever abstractions.
- Keep the first implementation simple enough to debug.

## Documentation Rules

When making a meaningful architectural or product decision, update `DECISIONS.md`.

When discovering something useful through testing or experimentation, update `LEARNINGS.md`.

When DSP behavior changes, update `docs/dsp-notes.md`.

When MPE behavior changes, update `docs/mpe-behavior.md`.

When starting a larger task, create or update a file in `plans/`.

When handing work to a reviewer agent or another machine, update `.agent/session.json` and create a concise note under `.agent/handoffs/`.

When this project teaches a reusable Studio Pattern lesson, record the local evidence first, then send a cross-project feedback block using `.agent/workflows/cross-project-feedback.md`.

## Human Verification

Assume the human wants to verify each meaningful milestone before the next large step.

Before continuing from one milestone to the next, leave the project in a buildable state and describe exactly what should be manually tested.
